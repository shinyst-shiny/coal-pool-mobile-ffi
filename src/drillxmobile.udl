namespace drillxmobile {
    DxSolution dx_hash(sequence<u8> challenge, u64 cutoff, u64 start_nonce, u64 end_nonce);
    DxGeneratedKey dx_generate_key();
};

dictionary DxSolution {
    sequence<u8> nonce;
    sequence<u8> digest;
    u32 nonces_checked;
    u32 difficulty;
};

dictionary DxGeneratedKey {
    string word_list;
    bytes keypair;
};


